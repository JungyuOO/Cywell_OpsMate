# v0.0.24 Handoff

## 우선순위

1. ConsolePlugin diagnostics frontend bundle
   - Add the actual CYOps diagnostics view served through the ConsolePlugin backend.
   - Consume `/api/ops/diagnostics` and `/api/ops/diagnostics/schema`.
   - Avoid any extra OAuth redirect in the Web Console path.

2. ConsolePlugin navigation/launcher integration
   - Add a CYOps-only diagnostics entry point in the plugin UI.
   - Keep Red Hat OpenShift Lightspeed viewer untouched.
   - Keep fallback admin Route out of the normal user journey.

3. Live cluster evidence
   - Run the OpenShift smoke script with and without `-EnableFallbackRoute`.
   - Record sanitized output in the phase doc.

## 완료 기준 제안

- A user already logged into OpenShift Web Console can open CYOps diagnostics without another login redirect.
- Diagnostics renders aggregate retrieval/document/re-embedding state.
- Fallback admin Route remains optional and documented as direct operational access only.
