# v0.0.29 Handoff

## Priority

1. Confirm manager image publication
   - Check the `manager-image` GitHub Actions run.
   - Confirm `ghcr.io/jungyuoo/cywell-opsmate:v0.0.28` manifest is available.

2. Apply Operator manifests
   - Run `kubectl kustomize config/default | oc apply -f -`.
   - Confirm `cywell-opsmate-controller-manager` rolls out in `cywell-opsmate-system`.

3. Create or update `OpsMateConfig`
   - Apply `config/samples/opsmate_v1alpha1_opsmateconfig.yaml` or a sanitized cluster-specific CR.
   - Confirm appserver, PostgreSQL, ConsolePlugin, and optional auth proxy resources reconcile.

4. Capture Web Console evidence
   - Open the CYOps ConsolePlugin diagnostics view.
   - Confirm Red Hat OpenShift Lightspeed remains separate.
   - Confirm no extra OAuth redirect occurs in the normal Web Console path.

## Proposed Completion Criteria

- Manager image is published and pullable.
- Operator manager pod is Available.
- `OpsMateConfig` reconciles CYOps resources.
- Sanitized Web Console screenshot and network evidence are recorded.
