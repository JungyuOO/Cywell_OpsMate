# v0.0.25 Handoff

## 우선순위

1. Browser verification for the diagnostics view
   - Run the appserver locally and open `/console-plugin/diagnostics`.
   - Verify the view loads and renders diagnostics using same-origin API calls.
   - Capture screenshot evidence.

2. OpenShift ConsolePlugin integration evidence
   - Deploy the Operator to a test OpenShift cluster.
   - Confirm CYOps diagnostics opens from Web Console without an extra OAuth redirect.
   - Record sanitized console/network evidence.

3. Frontend bundle pipeline decision
   - Decide whether to keep zero-toolchain static serving or introduce a small build pipeline.
   - If adding dependencies, document why and pin versions.

## 완료 기준 제안

- Local browser evidence exists for `/console-plugin/diagnostics`.
- OpenShift Web Console evidence confirms no extra OAuth redirect.
- Next frontend build strategy is documented.
