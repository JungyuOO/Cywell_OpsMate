# v0.0.23 Handoff

## 우선순위

1. ConsolePlugin admin diagnostics view
   - Add a CYOps-only diagnostics page or panel that calls `/api/ops/diagnostics`.
   - Display retrieval metrics, document aggregate state, re-embedding availability, and admin Route status.
   - Do not alter, hide, or replace the Red Hat OpenShift Lightspeed viewer.

2. OpenShift live smoke execution evidence
   - Run `deploy/scripts/openshift-v022-smoke.ps1` against the target cluster.
   - Capture sanitized output and attach it to the version phase doc.
   - Record any cluster-specific OAuth proxy image or cookie Secret requirements.

3. Diagnostics hardening
   - Add response schema tests for future UI consumption.
   - Add explicit no-secret assertions for additional fields if diagnostics expands.

## 완료 기준 제안

- CYOps diagnostics has a visible ConsolePlugin entry point.
- Live OpenShift smoke evidence is recorded.
- Diagnostics remains aggregate-only and secret-free.
