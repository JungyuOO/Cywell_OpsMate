# v0.0.31 Handoff

## Priority

1. Confirm catalog publish
   - Check the `operator-catalog` GitHub Actions run.
   - Confirm `ghcr.io/jungyuoo/cywell-opsmate-catalog:v0.0.30` manifest is available.

2. Local/CRC OLM install
   - Apply `deploy/olm/install/catalogsource.yaml`.
   - Wait for `cywell-opsmate-catalog` to become healthy in `openshift-marketplace`.
   - Apply namespace, OperatorGroup, and Subscription manifests.
   - Approve the generated InstallPlan manually.

3. Local/CRC resource verification
   - Confirm CSV reaches `Succeeded`.
   - Confirm manager Deployment is running.
   - Apply a sanitized `OpsMateConfig`.
   - Confirm appserver, PostgreSQL, and ConsolePlugin resources reconcile.

4. Local Web Console evidence
   - Open the CRC Web Console.
   - Confirm CYOps appears separately from Red Hat OpenShift Lightspeed.
   - Confirm no extra OAuth redirect occurs inside the normal Web Console path.

## Proposed Completion Criteria

- Local/CRC OLM install succeeds from CatalogSource/Subscription.
- `OpsMateConfig` reconciles expected resources.
- Sanitized local screenshot/network evidence is recorded.
- Real-server deployment remains deferred until local OLM evidence is complete.
