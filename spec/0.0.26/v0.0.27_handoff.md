# v0.0.27 Handoff

## Priority

1. Image publication
   - Tag and push the appserver image built from `deploy/containerfiles/appserver.Containerfile`.
   - Confirm the migration command remains executable as `cyops-pgvector-migrate`.

2. OpenShift Web Console live evidence
   - Deploy the Operator and appserver image to a test OpenShift cluster.
   - Open CYOps diagnostics from the Web Console.
   - Confirm the Web Console path does not add an extra OAuth redirect.

3. Sanitized evidence capture
   - Capture a sanitized screenshot of the CYOps diagnostics view.
   - Capture sanitized network evidence showing same-origin backend calls.

## Proposed Completion Criteria

- The appserver image is available to the test cluster.
- The CYOps diagnostics view renders from the Web Console backend path.
- Network evidence confirms no fallback Route usage.
