# v0.0.32 Handoff

## Priority

1. Confirm v0.0.31 images were published
   - `ghcr.io/jungyuoo/cywell-opsmate:v0.0.31`
   - `ghcr.io/jungyuoo/cywell-opsmate-bundle:v0.0.31`
   - `ghcr.io/jungyuoo/cywell-opsmate-catalog:v0.0.31`

2. Refresh CatalogSource on CRC
   - `oc apply -f deploy/olm/install/catalogsource.yaml`
   - Confirm `cywell-opsmate-catalog` is healthy in `openshift-marketplace`.

3. Review and approve the upgrade InstallPlan
   - Existing local CSV may be `cywell-opsmate.v0.0.29`.
   - Confirm the next InstallPlan references `cywell-opsmate.v0.0.31`.
   - Confirm related images:
     - `ghcr.io/jungyuoo/cywell-opsmate:v0.0.31`
     - `ghcr.io/jungyuoo/cywell-opsmate-appserver:v0.0.27`
   - Approve manually.

4. Retry local reconcile
   - Confirm CSV reaches `Succeeded`.
   - Confirm manager Deployment becomes Available.
   - Re-apply `kubectl kustomize deploy/olm/local-crc | oc apply -f -`.
   - Confirm appserver, PostgreSQL, and ConsolePlugin resources reconcile.
   - If manager crashes again, collect the now-initialized controller logs and fix root cause.

5. Capture local Web Console evidence
   - Confirm CYOps appears separately from Red Hat OpenShift Lightspeed.
   - Confirm no extra OAuth redirect occurs in the normal Web Console path.

## Proposed Completion Criteria

- CRC OLM install reaches CSV `Succeeded`.
- `OpsMateConfig` reconciles expected resources.
- Local Web Console screenshot/network evidence is sanitized and recorded.
- Real-server deployment remains deferred until local evidence is complete.
