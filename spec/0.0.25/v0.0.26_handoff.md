# v0.0.26 Handoff

## Priority

1. OpenShift Web Console live evidence
   - Deploy the Operator and appserver image to a test OpenShift cluster.
   - Open CYOps diagnostics from Web Console.
   - Confirm no extra OAuth redirect occurs in the Web Console path.

2. OpenShift screenshot and network evidence
   - Capture a sanitized screenshot of the CYOps diagnostics view.
   - Capture sanitized network evidence showing same-origin backend calls.

3. Packaging entrypoint
   - Wire `cmd/appserver` into the appserver container image.
   - Ensure `cyops-pgvector-migrate` remains available for migration Jobs.

## Proposed Completion Criteria

- Web Console diagnostics evidence is recorded.
- Network evidence confirms no fallback Route usage.
- Appserver image has a clear runtime entrypoint.
