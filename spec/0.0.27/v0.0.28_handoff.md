# v0.0.28 Handoff

## Priority

1. Deploy merged image defaults to OpenShift
   - Confirm the `appserver-image` GitHub Actions workflow published `v0.0.27`.
   - Apply the Operator manifests to the test cluster.
   - Create or update an `OpsMateConfig` that points to the published appserver image defaults.

2. Capture Web Console UI evidence
   - Open the CYOps ConsolePlugin diagnostics view from the OpenShift Web Console.
   - Confirm the Red Hat OpenShift Lightspeed viewer remains separate.
   - Confirm CYOps does not trigger an extra OAuth redirect in the Web Console path.

3. Capture sanitized network evidence
   - Record same-origin backend calls to `/api/ops/diagnostics` and `/api/ops/diagnostics/schema`.
   - Do not commit tokens, cookies, usernames beyond sanitized admin role context, or raw cluster identifiers.

## Proposed Completion Criteria

- CYOps diagnostics renders in the OpenShift Web Console.
- Sanitized screenshot and network evidence are recorded.
- No fallback Route is used for the normal Web Console user journey.
