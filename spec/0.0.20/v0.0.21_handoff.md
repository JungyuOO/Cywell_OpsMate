# v0.0.21 Handoff

## 우선순위

1. OpenShift OAuth proxy and Route shape
   - Add appserver exposure that terminates through a trusted OpenShift auth layer.
   - Ensure forwarded identity headers cannot be spoofed by direct unauthenticated traffic.
   - Document required OpenShift group binding for CYOps admins.

2. Migration Job reconciliation
   - Reconcile or lookup `PGVectorMigrationJob` when migration is approved.
   - Apply `ApplyPGVectorMigrationJobStatus` from live Job evidence.
   - Keep migration execution explicit and auditable.

3. Console diagnostics entry point
   - Add CYOps admin/debug placement for retrieval metrics and re-embedding state.
   - Do not alter or hide Red Hat OpenShift Lightspeed viewer.

## 완료 기준 제안

- Appserver admin APIs are reachable through a trusted OpenShift auth path.
- Direct unauthenticated admin access is not possible.
- Live migration Job status updates `OpsMateConfig.status.pgVectorReady`.
- Console diagnostics can show retrieval/re-embedding state without exposing secrets.
