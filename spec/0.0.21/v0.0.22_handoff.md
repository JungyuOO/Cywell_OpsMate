# v0.0.22 Handoff

## 우선순위

1. OpenShift live admin auth smoke
   - Add a script or documented command sequence to create the OAuth cookie Secret.
   - Apply a sample `OpsMateConfig` with `adminAuthProxyEnabled=true`.
   - Validate the Route redirects to OpenShift login and forwards identity headers to the appserver.

2. Migration Job operational smoke
   - Add a cluster smoke command that waits for `PGVectorMigrationJob` completion.
   - Fetch sanitized Job logs and assert DSNs/tokens are not printed.
   - Verify `OpsMateConfig.status.pgVectorReady` updates from live Job evidence.

3. Console diagnostics surface
   - Add a CYOps admin diagnostics view or endpoint that links retrieval metrics, re-embedding state, and admin Route state.
   - Do not alter, hide, or replace the Red Hat OpenShift Lightspeed viewer.

## 완료 기준 제안

- Admin Route has a repeatable OpenShift smoke procedure.
- Migration Job has a repeatable OpenShift smoke procedure.
- Diagnostics expose operational state without secrets.
