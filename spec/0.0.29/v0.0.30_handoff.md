# v0.0.30 Handoff

## Priority

1. Confirm bundle publish
   - Check the `operator-bundle` GitHub Actions run.
   - Confirm `ghcr.io/jungyuoo/cywell-opsmate-bundle:v0.0.29` manifest is available.

2. Add catalog image generation
   - Generate an OLM catalog image that includes the CYOps bundle.
   - Publish the catalog image to GHCR.
   - Add `CatalogSource` manifest that points at the catalog image.

3. Install through OLM
   - Apply namespace and OperatorGroup.
   - Apply CatalogSource in `openshift-marketplace`.
   - Apply Subscription in `cywell-opsmate-system`.
   - Approve the InstallPlan manually.

4. Capture Web Console evidence
   - Confirm CYOps appears as its own ConsolePlugin surface.
   - Confirm Red Hat OpenShift Lightspeed remains separate.
   - Confirm no extra OAuth redirect occurs in the normal Web Console path.

## Proposed Completion Criteria

- Bundle and catalog images are published.
- OLM Subscription reaches installed state.
- Manager pod is running from OLM-created resources.
- Sanitized Web Console screenshot and network evidence are recorded.
